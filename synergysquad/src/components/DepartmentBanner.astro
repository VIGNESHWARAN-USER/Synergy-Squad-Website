---
// src/components/DepartmentBanner.astro
---

<div class="banner-container">
    <h2 class="banner-text">Department of Computer Science and Engineering</h2>
</div>

<style>
    .banner-container {
        background-color: #111827; /* Dark background */
        padding: 0.75rem 1.5rem;
        text-align: center;
        border-bottom: 1px solid #374151;
    }
    

    .banner-text {
        margin: 0;
        font-size: 1rem;
        font-weight: 500;
        letter-spacing: 0.05em;
        /* --- START: CORRECTED TEXT COLOR --- */

        /* This is the base color, now much brighter */
        color: #e5e7eb; 

        /* The gradient now uses brighter colors for the shimmer */
        background-image: linear-gradient(
            -75deg,
            #e5e7eb 35%,
            #ffffff 50%,
            #e5e7eb 65%
        );
        
        background-size: 300% 100%;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-fill-color: transparent;
        
        animation: shimmer 7s linear infinite;
    }

    @keyframes shimmer {
        from { background-position: 100% 0; }
        to { background-position: 0% 0; }
    }
    /* --- END: CORRECTED TEXT COLOR --- */
</style>